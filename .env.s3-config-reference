# ============================================
# S3 COMPATIBLE STORAGE CONFIGURATION
# ============================================
# Cấu hình cho Longvan S3 (Other S3 Compatible Service)

# 1. FILESYSTEM DISK - Set to 's3' to use cloud storage
FILESYSTEM_DISK=s3

# 2. ACCESS KEY - Your S3 Access Key ID
AWS_ACCESS_KEY_ID=82AXCXFM1YRKXH1J8CAJ

# 3. SECRET KEY - Your S3 Secret Access Key (REQUIRED - currently empty)
AWS_SECRET_ACCESS_KEY=YOUR_SECRET_KEY_HERE

# 4. BUCKET - The bucket name to store files
AWS_BUCKET=phongland

# 5. REGION - Custom region for Longvan
AWS_DEFAULT_REGION=HCM2

# 6. CUSTOM ENDPOINT - The S3-compatible API endpoint
AWS_ENDPOINT=https://s3-hcm5-r1.longvan.net

# 7. PATH STYLE ENDPOINT - Must be 'true' for S3-compatible services
AWS_USE_PATH_STYLE_ENDPOINT=true

# Optional: Public URL (if different from endpoint)
# AWS_URL=https://s3-hcm5-r1.longvan.net/phongland
